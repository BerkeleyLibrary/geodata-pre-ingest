from setuptools import setup

setup(
    name='geodata_pre_ingest',
    description='Package for building script tools in ArcGIS Desktop to process ESRI ISO, ISO19139,Geoblacklight metadata',
    version='1.0.0',
    author='Yuchai Zhou',
    author_email='zhouyu@berkeley.edu',
    packages = ["geodata_pre_ingest"]
)
